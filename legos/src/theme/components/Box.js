export const Box = {
    // 1. We can update the components base styles
    baseStyle: {
        borderRadius: '2px',
        height: '100%',
        width: '100%'
    },
    // 2. We can add a new button size or extend existing
    sizes: {
        sm: {},
        md: {}
    }
};
